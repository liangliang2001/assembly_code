DATA SEGMENT
S DW 101H
SUM DW 2 DUP(?)
DATA ENDS
CODE SEGMENT
    ASSUME CS:CODE,DS:DATA
START:
    MOV AX,DATA
    MOV DS,AX
    MOV AX,S 
    CALL SQUR
    MOV SUM,CX
    MOV SUM+2,AX
    MOV AH,4CH
    INT 21H
SQUR PROC NEAR
    PUSH DX
    MOV CX,0
    MOV DX,1
REP1: CMP AX,DX
    JB EXIT
    SUB AX,DX
    INC CX
    ADD DX,2
    JMP REP1
EXIT: POP DX
    RET
SQUR ENDP
CODE ENDS
END START