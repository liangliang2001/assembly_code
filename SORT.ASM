DATA SEGMENT
A DW 20,8,3,16,234,567,88,231,23,54,32
N EQU ($-A)/2
DATA ENDS
CODE SEGMENT
    ASSUME CS:CODE,DS:DATA
START:
    MOV AX,DATA
    MOV DS,AX
    MOV CX,N
    MOV DI,CX
LOOP1:
    DEC DI
    JZ EXIT
    MOV CX,DI
    MOV BX,0
    MOV SI,1
LOOP2:
    MOV AX,A[BX]
    CMP AX,A[BX+2]
    JGE COTIUE
    XCHG AX,A[BX+2]
    MOV A[BX],AX
    MOV SI,0
COTIUE:
    ADD BX,2
    LOOP LOOP2
    CMP SI,0
    JE LOOP1
EXIT:
    MOV AH,4CH
    INT 21H
CODE ENDS
END START