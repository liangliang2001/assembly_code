CODE SEGMENT
    ASSUME CS:CODE
START:
    CALL DIB
    CALL CRLF
    CALL DISP
    CALL CRLF
    JMP START
DIB PROC NEAR
    MOV BX,0
REP1: MOV AH,1
    INT 21H
    SUB AL,30H
    JL EXIT
    CMP AL,9
    JG EXIT
    CBW
    XCHG AX,BX
    MOV CX,10
    MUL CX
    ADD BX,AX
    JMP REP1
EXIT: RET
DIB ENDP

DISP PROC NEAR
    PUSH AX
    PUSH CX
    PUSH DX
    MOV CH,4
REP2: MOV CL,4
    ROL BX,CL
    MOV DL,BL
    AND DL,0FH
    ADD DL,30H
    CMP DL,3AH
    JL PRIN
    ADD DL,7
PRIN: MOV AH,2
    INT 21H
    DEC CH
    JNZ REP2
    POP DX
    POP CX
    POP AX
    RET
DISP ENDP
CRLF PROC NEAR
    MOV DL,0DH
    MOV AH,2
    INT 21H
    MOV DL,0AH
    MOV AH,2
    INT 21H
    RET
CRLF ENDP
CODE ENDS
    END START